# Attribution

`main.cpp`, `MyMesh.h`, `MyMesh.cpp`, `UITask.h` and `UITask.cpp` in this directory are
derived from `examples/simple_room_server` in [MeshCore](https://github.com/meshcore-dev/MeshCore)
(commit c940ea5f, MIT License, Copyright (c) MeshCore contributors), extended with the
MeshXpress hooks. `lib/ed25519` is a symlink into the MeshCore tree and is not part of this
repository; that library is by Orson Peters under the zlib license.
